# คัดลอกไฟล์นี้เป็น .env แล้วใส่ค่าจริง

# MongoDB Atlas connection string (ไม่บังคับ — ถ้าไม่มีจะใช้ in-memory cache)
MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.xxxxx.mongodb.net/lottery?retryWrites=true&w=majority

# Port สำหรับ local dev (Vercel ไม่ใช้ค่านี้)
PORT=3000
